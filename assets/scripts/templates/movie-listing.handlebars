{{#each movies as |movie|}}

  <!-- {{> partial}} -->
  <!--Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <p data-id="{{movie.id}}">{{movie.name}} <button type="button" class="btn btn-success btn-sm">Add to my list</button><br/>{{movie.year}}<br/>  {{movie.rating}} Stars</p>
  <div>
    <!-- {{#if movie.description}}
      {{movie.description}}
    {{else}}
      No description.
    {{/if}} -->
  </div>

{{/each}}
